package com.oop.a09.interfacesandenums;

//interface is an abstract class that is used to group methods with empty bodies
public interface Animal {
    public void countLegs(); //interface method that doesn't have a body
    void sound(); // interface methods are public by default. You can not make them private
}
